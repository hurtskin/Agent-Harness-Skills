# 排期清单 — v1 → v2 待升级 / 待排期事项

> **职责**: 本文件是**待办看板（Kanban）**，统一登记 spec-00 / spec-01 / spec-02 / spec-03 / spec-04 所有 sub spec 中「未来再进行升级」/「v2 评估」/「⚠️ 建议未钉死」/「预留但未实现」等**未完成**事项。母会话接到新任务时先扫本清单 → 看新任务是否可顺便推进 / 闭环某项。
>
> **维护规则**（soul.md §11.5 + lessons §29）：
> - **未来升级项** → 进排期清单（append 到文件末尾）：每条排期含「现状 / 为什么升级 / 升级怎么改 / 升级优点」4 段 + ☐ 待做勾选框 + 优先级 H/M/L + 编号 #NNN
> - **已完成项** → 从排期清单**清除**（**不**保留闭环记录在本文件）：闭环信息**仅**记录在决策日志（append vN 索引含「闭环：排期清单 #NNN」字样）；决策日志是闭环事实唯一来源
> - **新任务到达**：母会话扫排期清单 → 看新任务是否可顺便推进 / 闭环某项
> - **扫 spec 时发现新排期项**：任何 spec 三件套 / 决策日志 / 排查报告 / 用户对话提到「v2 评估」/「未来」/「不实现」/「⚠️ 建议」等 → 母会话审阅时扫本清单 → 未登记则 append 1 项到对应分组
>
> **优先级**: H = 高（v2 立项时必做）/ M = 中（v2 立项时按需）/ L = 低（v2 立项后再讨论）

**最后更新**: <YYYY-MM-DD>（四件套初始化）
**总排期项数**: 0 项（初始空白，由 agent 在项目实战中按 spec 审查 / 决策日志新增项扫描时 append）

---

## 一、spec-00-arch（数据契约层）

<!-- agent 在项目实战中 append；不预填 -->

## 二、spec-01-relay（后端 FastAPI 中继层）

<!-- agent 在项目实战中 append；不预填 -->

## 三、spec-02-runner（前端 SDK 浏览器侧）

<!-- agent 在项目实战中 append；不预填 -->

## 四、spec-03 / spec-04（如有）

<!-- agent 在项目实战中 append；不预填 -->

---

## 维护规则摘要

- **新增排期项** → append 到文件末尾（按 spec 分组）
- **已完成排期项** → 从本文件**清除**（**不**保留闭环记录在本文件）；闭环信息**仅**记录在决策日志（`决策日志.md` append vN 索引含「闭环：排期清单 #NNN」字样）；决策日志是闭环事实唯一来源
- **新任务到达时** → 母会话扫本清单 → 看新任务是否可顺便推进 / 闭环某项
- **扫 spec 时发现新排期项** → 母会话审阅时扫本清单 → 未登记则 append 1 项到对应分组

详细规则见 [soul.md §11.5](file:///) + [lessons-learned.md §29](file:///)。